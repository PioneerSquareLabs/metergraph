[pytest]
# core/tests and server/tests each contain a test_catalog.py. importlib import
# mode lets pytest collect same-named test modules across the two distributions
# without requiring test packages.
addopts = --import-mode=importlib
